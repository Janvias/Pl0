const a = 5;
var x, y;
procedure p
    var z;
    begin
        z := 1
    end;
begin
    x := 10
    y := 20;
    if x > 5
        write(x);
    while x > 0
        x := x - 1;
    call q;
    write(y)
end.
